#include "config.h"
#include "utilizador.h"

int main(int argc, char const *argv[])
{
    // loadConfig();
    // printConfig();
    // freeMemory();

    while (1) // jony
    {
        entraParque();
    }

    return 0;
}
